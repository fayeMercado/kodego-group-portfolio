import React from 'react';
import PropTypes from 'prop-types';
import styles from './ProfileMarwin.module.css';

const ProfileMarwin = () => (
  <div className={styles.ProfileMarwin} data-testid="ProfileMarwin">
    ProfileMarwin Component
  </div>
);

ProfileMarwin.propTypes = {};

ProfileMarwin.defaultProps = {};

export default ProfileMarwin;
